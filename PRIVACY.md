# 隐私说明

更新日期：2026-07-15

糕糕桌宠被设计为完全离线运行的本地应用。

## 应用不会做什么

- 不创建账户，也不要求登录；
- 不包含遥测、分析、广告或崩溃上报；
- 不连接云端 AI、API、更新服务器或任何其他网络服务；
- 不上传设置、短句、鼠标位置或桌面内容；
- 不截取屏幕，不扫描个人文件，也不读取剪贴板。

## 本机保存的数据

应用只在操作系统提供的应用数据目录中保存实现功能所需的设置：

- 显示尺寸；
- 始终置顶、看向鼠标、自动漫步、气泡和开机启动开关；
- 用户添加或修改的本地短句；
- 糕糕最后一次显示的位置。

鼠标相对位置只用于在本机即时选择看向方向，不会写入日志或设置。启用“开机启动”时，应用会使用操作系统的登录启动项；关闭该选项会移除对应启动项。

## 删除数据

你可以在设置中恢复默认短句。卸载应用后，如需一并清除设置，可在系统的用户应用数据目录中删除标识为 `com.laozhongmihari.gaogao` 或“糕糕 / gaogao-desktop-pet”的应用数据。删除前请先退出糕糕。

## 第三方边界

从 GitHub Releases 下载应用时，GitHub、浏览器和操作系统可能按各自的隐私政策处理下载日志、安全检查或网络信息；这些行为不由糕糕桌宠应用发起或控制。

本说明若随功能变化而更新，会在仓库提交记录和后续 Release 中公开。应用若未来加入任何联网功能，将在启用前更新本说明并明确告知用户；当前 beta 不包含此类功能。
