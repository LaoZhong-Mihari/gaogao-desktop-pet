# 隐私说明

更新日期：2026-07-16

糕糕桌宠被设计为完全离线运行的本地应用。

## 应用不会做什么

- 不创建账户，也不要求登录；
- 不包含遥测、分析、广告或崩溃上报；
- 不连接云端 AI、API、更新服务器或任何其他网络服务；
- 不上传设置、鼠标位置、拖入文件路径或桌面内容；
- 不截取屏幕，不扫描个人文件，也不读取剪贴板。

## 本机保存的数据

应用只在操作系统提供的应用数据目录中保存实现功能所需的设置：

- 显示尺寸；
- 吃猫条获得的成长比例；
- 始终置顶、偶尔注意、自动漫步和开机启动开关；
- 糕糕最后一次显示的位置。

文件进入糕糕窗口时，只会触发应用内置的原有注意动作；应用不会记录鼠标位置。喂食时，应用只读取用户明确拖进糕糕窗口的单个文件，并与内置猫条图片逐字节比较；不会扫描目录、移动、删除或修改文件。启用“开机启动”时，应用会使用操作系统的登录启动项；关闭该选项会移除对应启动项。

首次成功启动时，应用会在当前用户桌面创建一张“糕糕的猫条（拖给糕糕）.png”，并在应用数据目录保存一个已创建标记。用户删除猫条后，应用不会在每次启动时自动恢复；可从托盘或设置页手动重新放置。

## 删除数据

卸载应用后，如需一并清除设置和猫条创建标记，可在系统的用户应用数据目录中删除标识为 `com.laozhongmihari.gaogao` 或“糕糕 / gaogao-desktop-pet”的应用数据。桌面猫条是一张普通 PNG，可直接删除。删除前请先退出糕糕。

## 第三方边界

从 GitHub Releases 下载应用时，GitHub、浏览器和操作系统可能按各自的隐私政策处理下载日志、安全检查或网络信息；这些行为不由糕糕桌宠应用发起或控制。

本说明若随功能变化而更新，会在仓库提交记录和后续 Release 中公开。应用若未来加入任何联网功能，将在启用前更新本说明并明确告知用户；当前 beta 不包含此类功能。
