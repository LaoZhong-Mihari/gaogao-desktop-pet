{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Local-only permissions shared by the pet and settings windows",
  "windows": ["pet", "settings"],
  "platforms": ["macOS", "windows"],
  "permissions": [
    "core:default",
    "store:default",
    "autostart:default"
  ]
}
