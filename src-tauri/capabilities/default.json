{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Local-only permissions shared by the pet, bubble, and settings windows",
  "windows": ["pet", "bubble", "settings"],
  "platforms": ["macOS", "windows"],
  "permissions": [
    "core:default",
    "store:default",
    "autostart:default"
  ]
}
